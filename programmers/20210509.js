// My answer

function solution(t, r) {
    let answer = t.map((v,i)=>({
        num:v, prior:r[i], index:i
    }))
    .sort((a,b)=>{
        if (a.)
    })
    return answer;
}

console.log(solution([0,1,3,0],[0,1,2,3]))