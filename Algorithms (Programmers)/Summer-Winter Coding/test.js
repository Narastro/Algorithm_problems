function solution(w,h){
            let answer = Number.MIN_SAFE_INTEGER;
            return answer;
        }

        let arr = [10,13];
        for (let ac of arr){
            document.write(`${ac}`);
        }
solution(8,12);