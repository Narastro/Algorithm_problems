/* 2021.12.14. 2020 KAKAO BLIND RECRUITMENT
 * 자물쇠와 열쇠
 */

const moveKey = (x, y, N) => {
  return;
};

const rotate = (x, y, N) => {
  return { x: N - y - 1, y: N - x - 1 };
};

const solution = (key, lock) => {};
